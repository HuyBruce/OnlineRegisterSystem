# ===================================================================
# OnlineRegisterSystem - Application Configuration
# ===================================================================

# Active profile (use 'dev' for local development)
spring.profiles.active=dev

# Server Configuration
server.port=8080

# Application Name
spring.application.name=OnlineRegisterSystem
